"""gui — a calm, read-only web app to muse over the organized photo archive.

Nothing here writes to the photo database. All numbers come from the local
metadata DB; photo previews are fetched as small Graph thumbnails on demand.
"""
